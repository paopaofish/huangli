{
  "name": "Android Codespaces Env",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  // 可选功能（Java + 通用工具 + Git）
  "features": {
    "ghcr.io/devcontainers/features/java:1": {
      "version": "17",
      "jdkDistro": "ms",
      "installGradle": true,
      "gradleVersion": "9.3.1"
    },
    "ghcr.io/devcontainers/features/common-utils:2": {},
    "ghcr.io/devcontainers/features/git:1": {}
  },

  // 环境变量（同时作用于容器和 VS Code 终端）
  "containerEnv": {
    "ANDROID_HOME": "/usr/local/share/android-sdk",
    "ANDROID_SDK_ROOT": "/usr/local/share/android-sdk"
  },
  "remoteEnv": {
    "PATH": "${env:PATH}:/usr/local/share/android-sdk/cmdline-tools/latest/bin:/usr/local/share/android-sdk/platform-tools"
  },

  // VS Code 定制
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "naco-siren.gradle-language",
        "fwcd.kotlin",
        "esafirm.android-xml-format",
        "Google.android-studio"
      ],
      "settings": {
        "java.configuration.updateBuildConfiguration": "automatic",
        "kotlin.languageServer.enabled": true
      }
    }
  },

  // 初始化脚本（安装 Android SDK 并接受许可）
  "postCreateCommand": "set -e; sudo mkdir -p /usr/local/share/android-sdk/cmdline-tools && sudo chown -R vscode:vscode /usr/local/share/android-sdk && wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O /tmp/cmdline-tools.zip && unzip -q /tmp/cmdline-tools.zip -d /usr/local/share/android-sdk/cmdline-tools && mv /usr/local/share/android-sdk/cmdline-tools/cmdline-tools /usr/local/share/android-sdk/cmdline-tools/latest && rm /tmp/cmdline-tools.zip && echo 'y' | /usr/local/share/android-sdk/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1 && /usr/local/share/android-sdk/cmdline-tools/latest/bin/sdkmanager 'platform-tools' 'platforms;android-34' 'build-tools;34.0.0'"
}